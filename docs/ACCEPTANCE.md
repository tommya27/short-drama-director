# MVP 验收记录

日期：2026-09-19。以下区分自动测试、真实 HTTP 联调与尚待设备实测的项目。

## 已验证

- 标准启动：后端 `8200` 健康检查成功、前端 `5274` 返回 200；服务仅绑定 `127.0.0.1`。
- 重复执行 `start.ps1 -SkipInstall` 保持前后端 PID 不变。`stop.ps1` 已实测停止记录中的两个服务，再次启动成功。端口冲突时拒绝启动，不停止其他程序。
- 后端：`58 passed`，2 条第三方依赖弃用警告。覆盖候选/预览不修改正式状态、提交幂等、版本冲突、无效输入、字段锁定、完整 SceneSpec 保存与失败回滚、分支完整状态隔离、角色信息可见性、输出持久化与来源，以及生成过程中取消后的迟到结果处理。
- 新增分支读取测试：`GET /scenes/{id}?branch_id=...` 的场景设定与状态来自同一分支，切换视图不混用另一分支的设定。
- 前端 TypeScript 与 Vite 构建通过。3D 独立分包约 877 kB，仍有体积提示。
- 慢速循环进行 9 项模拟时钟检查：生成后待审、人工提交后延时两秒、停止/导航清理、迟到草稿丢弃、网络失败及 409 时停止；没有自动提交路径。
- 新源码不导入旧仓库路径，无旧小说导航、章节、大纲、风格、插件、设置或登录页面。
- FeiControl 固定 commit、实际参考范围、MIT 许可与上游署名链已记录；没有复制其代码或模型资产。

## 已完成的真实 HTTP 闭环

使用正在运行的独立后端，全程 `0` 次模型调用：

1. 从董事会一句话想法生成设定并创建场景。
2. 运行三轮、三位角色，共提交 9 个事件。
3. 修改首轮对白并锁定；预览前后正式状态完全相同。
4. 每轮重复提交同一幂等键，返回一致结果，正式版本只递增一次。
5. 从主线 r3 复制“法务先追问”分支，添加定向导演指令，再预览和提交；主线状态保持不变。
6. 由主线 9 个事件生成场次卡、剧本、分镜，三份输出均保留原始事件 ID。

演示场景：`scene_d5f679bcab8d`。本机入口：<http://127.0.0.1:5274/scene/scene_d5f679bcab8d?branch=main>。运行摘要保存在 `.runtime/demo.json`，剧情保存在独立 SQLite 数据库中。

## 场景显现实现范围

`scene_manifests/*.json` 由 Vite 自动加载，场景选择器同步读取。内置董事会、客栈、古宅、办公室与通用舞台。配置驱动区域坐标、相机、颜色、程序化布景清单、道具交互点和地图渲染模式。未知角色样式使用自有低多边形占位；没有外部 GLB/图像依赖。

2D/3D 和镜头切换仅修改 React 显示状态。WebGL 初始化失败、组件渲染错误或上下文丢失时有地图回退实现。

## 真实模型联调（2026-09-20）

运行模式改为 `live / offline` 双模式后，完成一次真实模型端到端联调（DeepSeek `deepseek-chat`，OpenAI 兼容接口）：

1. 启动时读取项目 `.env`，`GET /health` 与 `GET /api/v1/runtime` 均返回 `mode=live`、`generator=llm_role_agents`、`key_present=true`；响应只含模式、模型名与环境变量名，不含密钥。
2. 在独立端口与独立数据库上跑通完整闭环（不干扰正在运行的演示服务）：生成候选 → 预览 → 提交 → 重复提交。
3. 通过演示服务（8200）再跑一次：3 个角色候选耗时约 `4.8s`，`generation_source=llm_role_agents`，3 个事件 `source` 全为 `llm`，文本内容明显不同于离线规则模板。
4. 预览后正式 `revision` 仍为 `0`、正式事件数 `0`；提交后 `revision=1`、正式事件数 `3`；用同一幂等键重复提交返回 200 且 `revision` 保持 `1`。
5. 新增 `backend/tests/test_runtime_mode.py`（20 条）覆盖：无密钥默认离线、有密钥自动 live、显式环境变量优先、非法模式报错、**显式 live 缺密钥拒绝启动**、`.env` 不覆盖既有环境变量、JSON 容错解析、live/offline 两种来源标记（用桩客户端，不联网）。

## 美术素材 T1（2026-09-20）

文生图接入并实跑：

1. 新增 `backend/director_core/images.py`：Seedream（OpenAI 兼容 `/images/generations`）客户端 + 提示词构建 + 落盘与来源记录；`backend/tests/test_images.py` 12 条离线测试（桩客户端，不联网），全量 `70 passed`。
2. 真实生成两张素材：场景背板 `1024x1024 / 231KB`、角色立绘 `864x1152 / 160KB`，模型 `doubao-seedream-5-0-pro-260628`，耗时分别约 `33s` / `36s`。
3. 每张素材记录提示词、模型、尺寸、时间戳、`sha256`、字节数，写入 `data/assets/index.json`；`GET /api/v1/assets/{asset_id}` 返回图片字节（实测 `200` 且与源文件一致）。
4. 供应商拒绝 `768x1024` 尺寸（`HTTP 400 InvalidParameter`）后改为竖版 `864x1152`；客户端同时保留"尺寸被拒则退回默认尺寸"的兜底，并按**实际尺寸**记录。
5. 素材按"槽位"（场景+类型+对象）唯一、文件按内容哈希去重：重新生成同一槽位会覆盖该记录，但**不会顶掉其它场景的素材**（该行为由 `test_list_assets_filters_by_scene` 覆盖）。
6. 前端：舞台工具栏「生成美术素材」按钮，2.5D 背板铺底 + 立绘替换占位形象，3D 背板平面 + 立绘精灵，底部素材条悬停显示提示词与模型；`npm run build`（含 `tsc --noEmit`）通过。
7. 边界：图片仅作视觉层，不参与世界状态裁决；3D 中素材加载失败由 `ArtBoundary` 兜住，只丢素材层。

## 候选校验：字段级可见降级（2026-09-20）

真实模型会提出违反世界规则的字段（非法地点、拾取已被持有/不在场的道具、披露自己不知道的事实）。
此前这些会让**整轮**返回 422，作者拿不到任何候选。现改为**字段级可见降级**：

| 情况 | 现在的行为 | 事件里的记录 |
|---|---|---|
| `move_to` 不在场景地点表 | 留在原地，其余动作保留 | `移动目标：地点不在场景表中，已留在原地` + `proposed` |
| 移动目标与当前地点不连通 | 留在原地 | `移动目标：与当前地点不连通，已留在原地` |
| 拾取无主/不在场的道具 | 忽略该操作，动作与对白保留 | `道具取用：该道具不在当前地点或并非无主，已忽略该操作` |
| 转交未持有/接收者不在场 | 忽略转交 | `道具转交：<原因>，已忽略该操作` |
| 披露自己不知道或已锁定的事实 | 忽略该条披露 | `信息披露：角色尚不知道该事实，已忽略` |
| 行动目标角色不存在 | 清空目标 | `行动目标：角色不存在，已忽略该对象` |

仍然是硬错误（保留 422）的只有**结构性问题**：缺少动作文本、引用不存在的角色、同一步同一角色重复事件、字段类型错误。

复演证据（`scene_c020e6b6488a`，live 模式）：一轮 3 个候选，ceo 提议了非法地点、legal 提议了非法取用，
两者都在事件里留下降级记录，**整轮成功返回**；预览后 `revision=0`，提交后 `revision=1`、正式事件 3；
场次卡/剧本/分镜三种输出均 `200` 且各自带 3 个来源事件 ID。

## 开场生成与场景样式（2026-09-20）

- `backend/director_core/opening.py`：模型路径 + 规则回退；`_coerce_manifest` 做字段白名单
  （palette/zones/asset_set/camera_presets/interaction_points，且 asset_set 只能取渲染器认识的词表）。
  `backend/tests/test_opening.py` 8 条离线测试：模型路径、白名单过滤、坏结构丢弃、
  模型抛错回退、模型结构不合法回退、无密钥走规则、`DIRECTOR_OPENING_MODE=rule` 强制规则且不调用模型。
- 全量测试 `78 passed`；前端 `npm run build`（含 `tsc --noEmit`）通过。
- 真实联调（live，两个题材）：
  - 「华山之巅，两位旧友为同一把剑对峙，其中一人藏着当年山门失火的真相」
    → 来源 `llm`，剧名《华山剑影》，区域 崖边/山巅平台/石阶入口，
    `scene_key=mountain-top`，`asset_set=[outdoor,rock,pine,rail]`，palette 与 3 个 zone 坐标齐全。
  - 「游轮驾驶舱，船长与二副在暴风雨前争夺航向决定权」
    → 来源 `llm`，剧名《风暴航线》，区域 舵轮平台/海图桌区/通讯台，
    `scene_key=ship`，`asset_set` 含 `console,rail`（驾驶台形态）。
- 落库与出图：由华山开场建立场景 `scene_b45bd14c6b02`，`GET /scenes/{id}` 回读确认
  manifest（palette/zones/asset_set）与世界区域一致；T1 背板真实生成 `1024x1024 / 341KB`，
  提示词自动包含场景地点与设定。
- 3D 舞台新增室外/载具形态：`asset_set` 含 `outdoor` 时渲染地面、岩石、松树、护栏与驾驶台，
  室内元素（墙、会议桌、椅子、绿植、文件柜）自动隐藏；构建通过。

## 尚待人工/设备验证

- 完整浏览器逐项操作、不同窗口尺寸与不同 GPU 的 3D 外观/性能检查，特别是 WebGL 丢失后的实机回退。本轮不以构建或 HTTP 成功冒充全部视觉验收。
- 真实模型的生成质量、长时连续多轮的稳定性与费用未做系统评估：目前只验证了单轮 3 角色的联调闭环，不代表自由题材的成稿质量。
- 前端徽标的实机确认（应显示“真模型试演 · deepseek-chat”，离线时为“离线规则试演”）需要在浏览器中人工核对一次。
- 发布前仍需整理全部传递依赖的最终许可清单；当前记录的是实际查阅与使用范围。

## 回看与连续演示

正式提交会写入不可变 checkpoint。`GET /api/v1/scenes/{id}/replay` 读取指定分支的历史帧，不修改当前正式状态；前端导演台提供播放、暂停、上一刻、下一刻、速度和进度条。回看模式是只读的，候选草稿和导演提交按钮会停用。

连续回看根据相邻历史帧中角色位置变化计算展示时长；角色使用路径点和步进动画移动，道具跟随持有角色。回看不会调用模型，也不会把历史帧写回正式剧情。

## 复验命令

在项目根目录运行：

```powershell
$env:PYTHONPATH = (Resolve-Path backend/.depsclean).Path
$env:DIRECTOR_DB = Join-Path ([IO.Path]::GetTempPath()) ('director-test-' + [guid]::NewGuid().ToString('N') + '.db')
py -3.13 -m pytest -q backend/tests
Remove-Item Env:DIRECTOR_DB
cd web
npm run build
```
